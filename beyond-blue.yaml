name: Beyond Blue
game_slug: beyond-blue
version: Good Old Games Installer
slug: beyond-blue
runner: wine
year: 2020
 
script:
  game:
    exe: $GAMEDIR/drive_c/Games/Beyond Blue/BeyondBlue.exe
    prefix: $GAMEDIR

  files:
    - setup: N/A:Select the GOG installer

  installer:
    - task:
        name: create_prefix
        install_mono: false
        install_gecko: false
        prefix: $GAMEDIR

    - task:
        executable: $setup
        name: wineexec
        prefix: $GAMEDIR

