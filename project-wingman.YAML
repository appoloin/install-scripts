name: Project Wingman
game_slug: project-wingman
version: Good Old Games Installer
slug: project-wingman
runner: wine
year: 2020

script:
  game:
    exe: $GAMEDIR/drive_c/GOG Games/Project Wingman/ProjectWingman.exe
    prefix: $GAMEDIR
    
  files:
    - setup: N/A:Select installer


  installer:
    - task:
        name: create_prefix
        install_mono: false
        install_gecko: false
        prefix: $GAMEDIR

    - task:
        executable: $setup
        name: wineexec
        prefix: $GAMEDIR
