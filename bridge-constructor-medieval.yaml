name: Bridge Constructor Medieval
game_slug: bridge-constructor-medieval
version: Windows Games Installer
slug: bridge-constructor-medieval
runner: wine
year: 2014

script:
  game:
    exe: $GAMEDIR/drive_c/GOG Games/Bridge Constructor Medieval/Launch Bridge Constructor Medieval.lnk
    prefix: $GAMEDIR

  files:
    - setup: N/A:Select the game installer

  installer:
    - task:
        name: create_prefix
        install_mono: false
        install_gecko: false
        prefix: $GAMEDIR
  
    - task:
        executable: $setup
        name: wineexec
        prefix: $GAMEDIR
