name: Dirt 4
game_slug: dirt-4
version: Windows Installer
slug: dirt-4
runner: wine
year: 2017

script:
  game:
    exe: "$GAMEDIR/drive_c/Games/DiRT 4/dirt4.exe"
    prefix: $GAMEDIR

  files:
    - setup: N/A:Select the Setup file

  installer:
    - task:
        name: create_prefix
        install_mono: false
        install_gecko: false
        prefix: $GAMEDIR

    - task:
        executable: $setup
        name: wineexec
        prefix: $GAMEDIR
