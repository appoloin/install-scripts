name: Commander Keen 5 
game_slug: commander-keen-5-the-armageddon-machine
version: Installer
slug: commander-keen-5-the-armageddon-machine-installer
runner: dosbox


script:
  files:
    - game: N/A:Select the file Keen5.exe
  game:
    main_file: $GAMEDIR/commander_keen.conf
  installer:
    - execute:
        args: $GAMEDIR/HDD $GAMEDIR/HDD/keen5
        file: mkdir

    - write_file:
        content: '#!/bin/bash

          src=$1

          dst=$2
          
          src_path=$(echo "$1" | sed -r "s/(.+)\/.+/\1/")

          cp -avr "$src_path/."  "$dst"'
        file: $CACHE/bulkcopy.sh
    - chmodx: $CACHE/bulkcopy.sh

    - execute:
        args: $game $GAMEDIR/HDD/keen5
        file: $CACHE/bulkcopy.sh

    - write_file:
        content: '[cpu]

          cputype=386_slow
          
          [autoexec]

            mount C "$GAMEDIR/HDD" -t dir

            C:

            cd keen5
            
            keen5e.EXE
            
            exit'
        file: $GAMEDIR/commander_keen.conf