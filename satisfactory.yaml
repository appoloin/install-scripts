name: Satisfactory
game_slug: satisfactory
version: Good Old Games Installer
slug: satisfactory
runner: wine
year: 2024
 
script:
  game:
    exe: $GAMEDIR/drive_c/Games/Satisfactory/FactoryGameSteam.exe
    prefix: $GAMEDIR

  files:
    - setup: N/A:Select the GOG installer

  installer:
    - task:
        name: create_prefix
        install_mono: false
        install_gecko: false
        prefix: $GAMEDIR

    - task:
        executable: $setup
        name: wineexec
        prefix: $GAMEDIR
