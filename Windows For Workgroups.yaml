name: Windows for Workgroups
game_slug: windows-for-workgroups
version: Installer
slug: windows-for-workgroups-installer
runner: dosbox

script:
  files:
    - game: N/A:Select the file pcdizzy.exe 
  game:
    main_file: $GAMEDIR/windows-for-workgroups.conf
  installer:
    - execute:
        args: $GAMEDIR/HDD
        file: mkdir

    - execute:
        args: $GAMEDIR/FDD
        file: mkdir

    - write_file:
        content: '[autoexec]

          mount C "$GAMEDIR/HDD" -t dir

          C:
          
          exit'
        file: $GAMEDIR/windows-for-workgroups-extract.conf


