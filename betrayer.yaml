name: Betrayer
game_slug: betrayer
version: Installer
slug: betrayer
runner: wine

script:
  game:
    exe: $GAMEDIR/c_drive/Program Files (x86)/Blackpowder Games/Betrayer/Binaries/Win32/Betrayer.exe
    prefix: $GAMEDIR

  files:
    - setup: N/A:Select installer

  installer:
    - task:
        name: create_prefix
        install_mono: false
        install_gecko: false
        prefix: $GAMEDIR

    - task:
        executable: $setup
        name: wineexec
        prefix: $GAMEDIR

  wine:
    Desktop: true


