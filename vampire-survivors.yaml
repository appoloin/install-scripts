name: Vampire Survivors
game_slug: vampire-survivors
version: Good Old Games Installer
slug: vampire-survivors
runner: wine
year: 2021

script:
  files:
    - setup: N/A:Select setup file from GOG

  game:
    exe: "$GAMEDIR/drive_c/Games/Vampire Survivors/VampireSurvivors.exe"
    prefix: $GAMEDIR

  installer:
    - task:
        name: create_prefix
        install_mono: false
        install_gecko: false
        prefix: $GAMEDIR

    - task:
        executable: $setup
        name: wineexec
        prefix: $GAMEDIR

