name: The Operator
game_slug: the-operator--1
version: Good Old Games Installer
slug: the-operator--1
runner: wine
year: 2023

script:
  game:
    exe: $GAMEDIR/drive_c/GOG Games/The Operator/OPERATOR.exe
    prefix: $GAMEDIR

  files:
    - setup: N/A:Select installer

  installer:
    - task:
        name: create_prefix
        install_mono: false
        install_gecko: false
        prefix: $GAMEDIR

    - task:
        executable: $setup
        name: wineexec
        prefix: $GAMEDIR


