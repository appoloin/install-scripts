name: Sorcerer of Claymorgue Castle
game_slug: sorcerer-of-claymorgue-castle
version: Installer
slug:   installer
runner: dosbox

script:
  files:
    - game: N/A:Select Sherlock.com

  game:
    main_file: $GAMEDIR/play.conf

  installer:
    - execute:
        args: $GAMEDIR/HDD  $GAMEDIR/HDD/castle
        file: mkdir

    - write_file:
        content: '#!/bin/bash

          src=$1

          dst=$2
          
          src_path=$(echo "$1" | sed -r "s/(.+)\/.+/\1/")

          cp -avr "$src_path/."  "$dst"'
        file: $CACHE/bulkcopy.sh
    - chmodx: $CACHE/bulkcopy.sh

    - execute:
        args: $game $GAMEDIR/HDD/castle
        file: $CACHE/bulkcopy.sh

    - write_file:
        content: '[mouse]

            capture_mouse = onstart middlerelease  

            mouse_sensitivity= 15

          [autoexec]

            mount C "$GAMEDIR/HDD" -t dir

            cls 

            c: 

            cd castle 

            YOHO.EXE

            exit'
        file: $GAMEDIR/play.conf

