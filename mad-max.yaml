name: Mad Max
game_slug: mad-max
version: Good Old Games Installer
slug: mad-max
runner: wine
year: 2015
 
script:
  game:
    exe: $GAMEDIR/drive_c/GOG Games/Mad Max/Launch Mad Max.lnk
    prefix: $GAMEDIR

  files:
    - setup: N/A:Select the GOG installer

  installer:
    - task:
        name: create_prefix
        install_mono: false
        install_gecko: false
        prefix: $GAMEDIR

    - task:
        executable: $setup
        name: wineexec
        prefix: $GAMEDIR
