name: Saints Row
game_slug: saints-row--2
version: Xbox 360
slug: fsaints-row--2
runner: xenia-linux
year: 2006

script:
  files:
    - game:  N/A:Select file *.iso

  game:
    main_file: $GAMEDIR/saints-row.iso

  installer:
    - move:  #move and rename
        src: $game
        dst: "$GAMEDIR/saints-row.iso"
