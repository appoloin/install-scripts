name: kings_quest_8
game_slug: kings_quest_8
version: Installer
slug: kings_quest_8-installer
runner: wine

script:
  game:
    exe: $GAMEDIR/drive_c/SIERRA/Mask/Mask.exe
    arch: win32
    prefix: $GAMEDIR

  files:
    - setup: N/A:Select Setup.exe

  installer:
    - task:
        name: create_prefix
        arch: win32
        install_mono: false
        install_gecko: false
        prefix: $GAMEDIR

    - task:
        executable: $setup
        name: wineexec
        prefix: $GAMEDIR

  wine:
    Desktop: true

