name:  Darkest of Days
game_slug: darkest-of-days
version: Installer
slug: darkest-of-days
runner: wine

script:
  game:
    exe: $GAMEDIR/drive_c/Program Files/R.G. Mechanics/Darkest of Days/darkestofdays.exe
    arch: win32
    prefix: $GAMEDIR

  files:
    - setup: N/A:Select setup.exe

  installer:
    - task:
        name: create_prefix
        arch: win32
        install_mono: false
        install_gecko: false
        prefix: $GAMEDIR

    - task:
        executable: $setup
        name: wineexec
        prefix: $GAMEDIR
  wine:
    Desktop: true


