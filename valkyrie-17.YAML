name:  Valkyrie 17
game_slug: valkyrie-17
version: C64
slug: valkyrie-17
runner: flatpak
year: 1983

script:
  game:
    appid: net.sf.VICE
    args: $GAMEDIR/VALKYRIE.T64
    working_dir: $GAMEDIR

  files:
    - game: N/A:Select VALKYRIE.T64


  installer:
    - copy:
        src: $game
        dst: $GAMEDIR

    #trizbort launcher 
    - write_file:
        content: '#!/bin/bash
        
         flatpak-spawn --host env LUTRIS_SKIP_INIT=1 flatpak run net.lutris.Lutris lutris:rungame/trizbort

         '
        file: $GAMEDIR/trizbort.sh

    - execute:
        file: chmod
        args: +x  "$GAMEDIR/trizbort.sh"

  system:
    prelaunch_command: $GAMEDIR/trizbort.sh