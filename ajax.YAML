name: Ajax
game_slug:  ajax
version: ROM
slug: ajax
runner: mame
year: 1986

script:
  game:
    main_file: $GAMEDIR/ajax.zip
    args: -artpath "$GAMEDIR/bazel"

  files:
    - game: N/A:Select ajax.zip.
    - bazel1:  
        url: https://github.com/appoloin/Patches/raw/refs/heads/master/Art/ajax.zip
        filename: ajax.zip

  installer:
    - copy:
        src: $game
        dst: $GAMEDIR

    - execute:
        file: mkdir
        args: $GAMEDIR/bazel

    - move:
        src: $bazel1
        dst: "$GAMEDIR/bazel"

  mame:
    fullscreen: true
    uimodekey: "Right Alt"