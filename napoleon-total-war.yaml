name: Napoleon - Total War
game_slug: napoleon-total-war
version: Windows Installer
slug: napoleon-total-war
runner: wine
year: 2010

script:
  game:
    prefix: $GAMEDIR
    exe: $GAMEDIR/drive_c/MagiPacks/Shogun - Total War/ShogunM.exe

  files:
    - setup: N/A:Select the Setup file
    
  installer:
    - task:
        name: create_prefix
        install_mono: false
        install_gecko: false
        prefix: $GAMEDIR

    - task:
        executable: $setup
        name: wineexec
        prefix: $GAMEDIR
