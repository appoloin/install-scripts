name: Sacred Gold
game_slug: sacred-gold
version: Good Old Games
slug: sacred-gold
runner: wine
year: 2007

script:
  game:
    exe: $GAMEDIR/drive_c/Sacred Gold/Launch Sacred Gold.lnk
    prefix: $GAMEDIR

  files:
    - setup: N/A:Select installer

  installer:
    - task:
        name: create_prefix
        install_mono: false
        install_gecko: false
        prefix: $GAMEDIR


    - task:
        app: wmp9
        description: Installing additional components
        name: winetricks
        prefix: $GAMEDIR

    - task:
        executable: $setup
        name: wineexec
        prefix: $GAMEDIR

