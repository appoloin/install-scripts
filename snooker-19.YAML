name: Snooker 19
game_slug: snooker-19
version: Windows Installer
slug: snooker-19
runner: wine
year: 2007

script:
  game:
    exe: $GAMEDIR/drive_c/Games/Napoleon Total War/Napoleon.exe
    prefix: $GAMEDIR

  files:
    - setup: N/A:Select installer

  installer:
    - task:
        name: create_prefix
        install_mono: false
        install_gecko: false
        prefix: $GAMEDIR

    - task:
        executable: $setup
        name: wineexec
        prefix: $GAMEDIR

