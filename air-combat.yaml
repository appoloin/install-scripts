name: Air Combat
game_slug: air-combat
version: Playstation 1
slug: air-combat
runner: duckstation
year: 1995

script:
  files:
    - game:  N/A:Select file cue file

  game:
    main_file: '$GAMEDIR/Air Combat (Europe) (EDC).cue'

  installer:
    #copy everything
    - execute:
        command: 'src_path=$(echo "$game" | sed -r "s/(.+)\/.+/\1/")

          cp -avr "$src_path/."  "$GAMEDIR"

          '    
