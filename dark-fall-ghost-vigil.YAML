name: Dark Fall Ghost Vigil
game_slug: dark-fall-ghost-vigil
version: Good Old Games Installer
slug: dark-fall-ghost-vigil
runner: wine
year: 2020

script:
  game:
    exe: $GAMEDIR/drive_c/Program Files/Dark Fall Ghost Vigil/Dark Fall - Ghost Vigil.exe
    prefix: $GAMEDIR

  files:
    - setup: N/A:Select installer

  installer:
    - task:
        name: create_prefix
        install_mono: false
        install_gecko: false
        prefix: $GAMEDIR

    - task:
        executable: $setup
        name: wineexec
        prefix: $GAMEDIR

