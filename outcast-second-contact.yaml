name: Outcast - Second Contact
game_slug: outcast-second-contact
version: Good Old Games Installer
slug: outcast-second-contact
runner: wine
year: 2017

script:
  files:
    - setup: N/A:Select setup file 

  game:
    exe: "$GAMEDIR/drive_c/GOG Games/Outcast - Second Contact/Launch Outcast - Second Contact.lnk"
    prefix: $GAMEDIR

  installer:
    - task:
        name: create_prefix
        install_mono: false
        install_gecko: false
        prefix: $GAMEDIR
    
    - task:
        executable: $setup
        name: wineexec
        prefix: $GAMEDIR

