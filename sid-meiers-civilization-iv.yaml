name: Civilization IV
game_slug: sid-meiers-civilization-iv
version: MagiPacks Installer
slug: sid-meiers-civilization-iv
runner: wine
year: 2005

script:
  files:
    - setup: N/A:Select setup file

  game:
    exe: "$GAMEDIR/drive_c/MagiPacks/Civilization IV/Civilization4.exe"
    prefix: $GAMEDIR

  installer:
    - task:
        name: create_prefix
        install_mono: false
        install_gecko: false
        prefix: $GAMEDIR

    - task:
        executable: $setup
        name: wineexec
        prefix: $GAMEDIR
