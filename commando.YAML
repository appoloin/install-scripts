name: Commando
game_slug:  commando
version: ROM
slug: commando
runner: mame
year: 1985

script:
  game:
    main_file: $GAMEDIR/commando.zip
    args: -artpath "$GAMEDIR/bazel"

  files:
    - game: N/A:Select commando.zip.
    - bazel1:  
        url: https://github.com/appoloin/Patches/raw/refs/heads/master/Art/commando.zip
        filename: commando.zip


  installer:
    - copy:
        src: $game
        dst: $GAMEDIR

    - execute:
        file: mkdir
        args: $GAMEDIR/bazel

    - move:
        src: $bazel1
        dst: "$GAMEDIR/bazel"

  mame:
    fullscreen: true
    uimodekey: "Right Alt"