name: Bad Mojo - Redux
game_slug: bad-mojo-redux
version: Good Old Games Installer
slug: bad-mojo-redux
runner: wine

script:
  game:
    exe: "$GAMEDIR/drive_c/GOG Games/Bad Mojo/BADMOJO.EXE"
    arch: win32
    prefix: $GAMEDIR

  files:
    - setup: N/A:Select installer

  installer:
    - task:
        arch: win32
        name: create_prefix
        install_mono: false
        install_gecko: false
        prefix: $GAMEDIR

    - task:
        executable: $setup
        name: wineexec
        prefix: $GAMEDIR
