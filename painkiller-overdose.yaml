name: Painkiller - Overdose
game_slug: painkiller-overdose
version: Installer
slug: painkiller-overdose
runner: wine


script:
  game:
    exe:  drive_c/GOG Games/Painkiller - Overdose/Bin/Overdose.exe
    arch: win32
    prefix: $GAMEDIR
  files:
    - setup: N/A:Select the Setup.exe
  installer:
    - task:
        name: create_prefix
        arch: win32
        install_mono: false
        install_gecko: false
        prefix: $GAMEDIR

    - task:
        executable: $setup
        name: wineexec
        prefix: $GAMEDIR

  wine:
    Desktop: true

