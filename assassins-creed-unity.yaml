name: Assassin's Creed - Unity
game_slug: assassins-creed-unity
version: Good Old Games Installer
slug: assassins-creed-unity
runner: wine
year: 2014

script:
  files:
    - setup: N/A:Select setup file

  game:
    exe: "$GAMEDIR/drive_c/Games/Assassins Creed Unity/ACU.exe"
    prefix: $GAMEDIR

  installer:
    - task:
        name: create_prefix
        install_mono: false
        install_gecko: false
        prefix: $GAMEDIR

    - task:
        executable: $setup
        name: wineexec
        prefix: $GAMEDIR
