name:  Ghost of Tsushima
game_slug: ghost-of-tsushima
version: Windows Games
slug: ghost-of-tsushima
runner: wine
year: 2020

script:
  game:
    exe: $GAMEDIR/drive_c/Games/Outer Wilds/OuterWilds.exe
    prefix: $GAMEDIR

  files:
    - setup: N/A:Select installer

  installer:
    - task:
        name: create_prefix
        install_mono: false
        install_gecko: false
        prefix: $GAMEDIR

 
    - task:
        executable: $setup
        name: wineexec
        prefix: $GAMEDIR
