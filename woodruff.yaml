name: woodruff
game_slug: woodruff
version: Installer
slug:  woodruff-installer
runner: scummvm


script:
    files:
      - game:  N/A:Select the file *.stk

    game:
        game_id: gob
        path: $GAMEDIR

    installer:
    - write_file:
        content: '#!/bin/bash

        src=$1

        dst=$2

        src_path=$(echo "$1" | sed -r "s/(.+)\/.+/\1/")

        find "$src_path" -type f \( -iname "*.stk" -o -iname "*.ITK" \) -exec cp {} "$dst" \;'
        file: $CACHE/selectcopy.sh

    - chmodx: $CACHE/selectcopy.sh

    - execute:
        args: $game $GAMEDIR
        file: $CACHE/selectcopy.sh