name: Paradise Killer
game_slug: paradise-killer
version: Good Old Games Installer
slug: paradise-killer
runner: wine
year: 2020

script:
  files:
    - setup: N/A:Select setup file from GOG

  game:
    exe: "$GAMEDIR/drive_c/GOG Games/Paradise Killer/ParadiseKiller.exe"
    prefix: $GAMEDIR

  installer:
    - task:
        name: create_prefix
        install_mono: false
        install_gecko: false
        prefix: $GAMEDIR

    - task:
        executable: $setup
        name: wineexec
        prefix: $GAMEDIR

